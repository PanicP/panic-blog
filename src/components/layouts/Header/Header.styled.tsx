import styled from 'styled-components'
import { ResponsiveDiv } from '../Responsive/Responsive.styled'

export const StyledHeader = styled(ResponsiveDiv)`
    border-bottom: 1px solid lightgrey;
    min-height: 5rem;

    display: flex;
    align-items: center;
`

export const StyledLogo = styled.img``
